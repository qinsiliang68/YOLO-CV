# Local Primary Sources

Official landing-page snapshots, full-text HTML, supplements, and PDFs are stored
locally below this directory. Git ignores the source bytes; `SOURCE_ACQUISITION.csv`
and each paper note bind them by relative path, size, SHA-256, URL, and authority.
The Desktop mirror may include them only when its own manifest verifies every byte.
