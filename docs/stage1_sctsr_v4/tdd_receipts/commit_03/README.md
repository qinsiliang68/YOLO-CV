# Commit 03 TDD evidence

This directory records the failure-first and post-implementation evidence for
the common-parent checkpoint, branch-lineage, and logical-artifact-index
rollback unit. `RED.junit.xml` is executed against the untouched expert source
package; `GREEN.junit.xml` and `TARGETED_GREEN.junit.xml` are executed against
the repository implementation.
