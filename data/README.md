# Data Folder Policy

Do not place large image datasets in this repository folder.

All raw, intermediate and exported SewerML datasets should live under:

`C:\Sewer-ML\`

Current raw image pool:

`C:\Sewer-ML\sewerml_train_images\`

The repository keeps code, small manifests, and source annotation CSV files only.
